[ELIAS]
idee elias powershot ratio (je me comprends) (% de powershot)

Erreur quand on est sur une autre page (chat) et que le j2 \
	lance la game ca me redirige en ouvrant plusieurs fois le \
	WebSocket ce qui est pas clean

quand une game est fini on met isready a false et on enleve la task de ball

regarder si la communication entre ws et wss \
	sur le pong est normale ou si on utilise pas ws tout le temps

Fix le pb avec plusieurs jeux local qui semblent ne pas etre separer \
	car on recois quand meme le socket sur les autres comptes
	
[LEON]
Les messages du system info s'envoie autant de fois que le nombre de pelo dans le groupe de discution si j'ajoute qlq

Ajouter le invite to game qui est dans le chat prive \
	pour inviter un joueur a jouer

Ajouter le mode tournoi avec un systeme de bracket \
	et de matchs a jouer

Fix le pb quand on ctrl+r sur la page GameOver

Ajouter le canvas en responsive avec une regle transform css